{

    "index": {
        "GET": {
            "function": "index",
            "authorization": false,
            "parameters": []
        }
    },

    "send_event": {
        "POST": {
            "function": "send_event",
            "authorization": true,
            "parameters": [
                { "name": "queue", "kind": "enum", "required": true, "options": [ "opt1", "opt2" ] },
                { "name": "key", "kind": "string", "required": true, "options": [] },
                { "name": "event", "kind": "string", "required": true, "options": [] },
                { "name": "payload", "kind": "map", "required": true, "options": [] }
            ]
        }
    }

}